"""CropMath: objective benchmark toolkit for agricultural formula reasoning.

Public evaluation utilities for the CropMath benchmark:

    answer_parser  -- objective numeric answer extraction and tolerance scoring
    prompts        -- knowledge-ablation prompt builders for the six conditions

The released dataset (``release/cropmath-v1``) provides the curated mechanistic
formulas as mathematical expressions with anonymized identifiers. Dataset
generation and provenance tooling are kept by the benchmark maintainer.
"""
